{
  "name": "load-image",
  "version": "0.1.1",
  "author": "sasa+1 <sasaplus1@gmail.com>",
  "description": "load image",
  "main": "./index.js",
  "license": "MIT",
  "readmeFilename": "README.md",
  "repository": {
    "type": "git",
    "url": "https://github.com/sasaplus1-prototype/load-image.js.git"
  },
  "scripts": {
    "build": "npm run compile && npm run minify",
    "compile": "npm run webpack -- --output-file load-image.js",
    "minify": "npm run webpack -- --optimize-minimize --output-file load-image.min.js",
    "webpack": "webpack --colors --display-error-details --progress"
  },
  "dependencies": {
    "once": "github:sasaplus1-prototype/once.js"
  },
  "devDependencies": {
    "webpack": "^1.13.1"
  }
}
